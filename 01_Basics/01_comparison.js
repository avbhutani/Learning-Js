console.log('A' < 'a')

// Now we are using the ASCII values to compare all the types of data.
// Js allows us to compare the strings using the ASCII values.
// Whereas in typescript we cannot compare the strings using the ASCII values.
// In typescript we can only compare the strings using the alphabetical order.

console.log(null === 0)
console.log(null <= 0)
console.log(null == 0)